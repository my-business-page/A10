const l=document.querySelector('.loader');addEventListener('load',()=>setTimeout(()=>l?.classList.add('done'),300));const b=document.querySelector('#toggle'),n=document.querySelector('nav');b?.addEventListener('click',()=>n?.classList.toggle('open'));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in');else if(e.boundingClientRect.top>0)e.target.classList.remove('in')}),{threshold:.1});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));document.querySelectorAll('form').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();alert('Enquiry prepared. Please contact Black Kettle through its verified active channel to submit it.')}));